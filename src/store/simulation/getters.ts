// profile/getters.ts
import { GetterTree } from 'vuex';
import { SimulationState } from './types';
import { RootState } from '../types';

export const getters: GetterTree<SimulationState, RootState> = {
};